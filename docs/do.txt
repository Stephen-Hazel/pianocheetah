play____________________________________________________________________________

gather good songs - find BEST way to get sheet music in
play em;  study em;  pick out riffs;  change em;  make one;

especially magic singable dumb songs
   yellowbrickroad, petshopboys, yazoo/erasure, owl city
   journey - don't stop believin, who's crying now, patiently,
      feeling that way, just the same way, lovin touchin squeezin
      lights, stay awhile, wheel in the sky,

dup the good rush sounds


code____________________________________________________________________________

step entry
   show current duration, articulation in lyrics area
      when step entry keys are hit
   need to be in pause mode
      and only 1 track picked in record mode
   now time is where notes start
   duration - show string combo whq. etc
      3c# .   all toggle
      3d# 3
      3f# w
      3g# h
      3a# q
      4c# e
      4d# s
      4f# t
      4a# wipe  (empty dur string, set portato)
   articulation - show staccatissimo (1/4),
                       staccato (1/2),
                       portato (3/4),  default
                       legato (full)
      5c# artic down
      5d# artic up
   notes hit (3a-4b) add to a chord with picked dur
      white note goes flat/sharp with keysig in most recent octave
      hit note a 2nd time to remove
      3d octave down
      3e octave up
      3f flat
      3g sharp
      usually add long dur notes first, then change dur to less for advance
   3c advance time by lowest dur of the notes added

track ttips - show note range, and control list
   take drum/ outa sngrp, inst&kit in tip

dragdrop for midicfg, track, FLst

can reprpt make ez tracks repeat rhythms on same keys

changing device always pickin chan 1

syn's envelopes more - esp chorus-y things


spec____________________________________________________________________________

pcheetah
   controls in gui - pad of 3 control,values (3 x droplist,button)
      pick control.  down on button follows mouse x to set value

   device/drummap.txt picks drum layout for keyboards
         kick,kik2, snar,snr2, hhcl,hhop,hhpd, ride,rdbl, cras
                    clap,sstk
      drum map name in place of 4RH?

   do unreachable powerchords, guitar strum, rhythm guitar
      drums on keys - make a good map but get it from device/drummap.txt
         pick drum instead of oct (show Drum instead of 4RH)

   in oct, are black notes slightly offset from strict 12/7 by
      slight narrowing/offsetting for a more aesthetic look?

   when device is off can it fallback to syn?
      then autorestore once dev is avail?

   more obviously stacked notes somehow
   whistle compose?

syn
   in note ev, pass duration so syn can do echoes in tempo

   note level control of envelopes etc via "note color"
      palette of fx colors:
         name=> all defaults overriden w set of control to set at once
            ex: step5:     pBnR=5th pStp=key
                phaserlr:  envs=phaser|panl
      note color...  set only a note to phaserlr
         if color!=0 (str != '')
            note estr overrides chan = fxlist[color-1],

   follow an osc?

   env stuff:
      dur - discrete   *1/16 *2/16 *3/16 *4/16 .. *32/16
            discrete2  /16 /8 /4 /2 1 *2 *4 *8 *16
            continuous 0-1
      rand (sample and hold / osc follow)


doc_____________________________________________________________________________

all docs convert to video

videos on songs hearin/playin; keyCmd; hook up a track volume

shoot ez linux
   get your data backed up properly - exfat
   make an iso and install it

why is piano playin hard
   how many people should play a song
   how is guitar better

how video   cleanin up a song
   name it a.mid in a dir
   addin/removin cues,
   end of song controls makin it go long - can't auto kill em
   editing raw .song with text editor


later___________________________________________________________________________

bugs should only show on trill note 1, not 2+

dark/light mode flip - pcheetah
   flip to light - time area bg ok but fg still white from dark mode
                   and time still lite/dark without red pause showing
   lyr bg doesnt change but fg prob does?
   notes need a redraw
   track table doesn't redraw but midicfg's is ok

FLst - search on 2nd char within queue,learn,etc

if tsig is n/d/*, show _ e & a for /4  or _ & a for /3

pan draws weird lines off to the right on screen

find cheapest - weighted keyboard to chop  (roland rd64 too spendy)
     smallest keyboard - prob my akai

solo a track?  (mute all others)

Info() doesn't seem to work..

put sounds with banks in a tooltip (strip *,bank)

how is recorded tempo somehow bein saved to learn .song ?

hearmode rec - show notes/ctrls as played;  in poz, step recording

ez should make similar sections have same fingering
      restart at rep boundaries or somethin
   using 2 hands on a given track (for 10 notes instead of 5)
      pick ? 3+LH

arp random notes w/in chord

limit note editing in RCRD to only what makes sense
   dlgmov - not in RCRD;   is move to backing ok?
            upon close - "you need a practice track to add notes on"  ???

EvIns - do i ALWAYS keep trk[].e in order throughout .song lifetime?

control picker should show if control is used in song
   map cc zone1 to somethin how?
   hover should show track# etc
   pick all ctrls used in song

StrArr::Add,Ins callers need to USE new bool return code :/

set resolved drum sounds for syn
   (or have song2wav resolve drum sounds)

DrMap list should combine all devices' devtypes' drum.txt's
   unknown drum turns into x128
   should include orig drum in list

ctrl list
   show #events in song.  0 sorted to bottom
   take out tempo?
   preserve across song load
   give controls fullname too

control editing
   how to edit cues at nearly same time
   when hovering MsPos should find most recent ctl given time
      show it without [+]
      but which track if not global ctl?
      and does Draw do sep lines per track for non global ctls?
   editing tsig,tmpo,ksig
